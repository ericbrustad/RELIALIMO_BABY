.env
.env.local
.env.*
*.env
*.env.*
# secrets
.env.*.local

# NOTE: shared/env.js is committed because it contains only PUBLIC anon keys
# These are client-side keys meant to be exposed in the browser

# Environment backup files (SENSITIVE - NEVER COMMIT)
relialimo-env-backup*.json
ENV_SAVE_AND_RESTORE.html

.vercel
.env*.local

# Next.js
.next/
out/
node_modules/

.vercel
.env*.local
